---
import Layout from './Base/Base.astro';
import type { Props as BaseHeadProps } from '$components/Astro/BaseHead.astro';

export interface Props {
  seo?: BaseHeadProps;
}

const { seo } = Astro.props as Props;
---

<Layout seo={seo}>
  <slot />
</Layout>
