---
import { TracingBeam } from '../ui/tracing-beam';

const content = await Astro.slots.render('default');
---

<div class="relative">
  <TracingBeam client:only="react" contentHtml={content}>
    <slot />
  </TracingBeam>
</div>
