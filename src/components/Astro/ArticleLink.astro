---
type Props = {
  href: string;
};

const { href } = Astro.props;
const text = await Astro.slots.render('default');

const isImage = href.includes('.png') || href.includes('.jpg');
---

{isImage ? <img src={href} alt={text} class="rounded-md shadow" /> : <a href={href} set:html={text} />}
